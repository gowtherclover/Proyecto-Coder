{
  "name": "paquete-node-coder",
  "version": "1.0.0",
  "description": "paquete de node de coder",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build-css": "node-sass --include-path scss main.scss ../estilos.css --output-style compressed",
    "watch-css": "nodemon -e scss -x \"npm run build-css\""

  },
  "author": "Enzo Aguilar",
  "license": "ISC",
  "devDependencies": {
    "node-sass": "^7.0.3",
    "nodemon": "^2.0.20"
  }
}
